package detect

// ShouldSwitch determines if current word suggests switching from current layout.
// Placeholder: always false.
func ShouldSwitch(s string, current int) (bool, error) {
	return false, nil
}
